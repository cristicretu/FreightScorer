import { Box } from 'native-base';

export default function Separator() {
  return (
    <Box
      borderBottomColor="gray.200"
      borderBottomWidth={1}
      marginBottom={4}
      marginTop={2}
    />
  );
}
